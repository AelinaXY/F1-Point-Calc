info:
  name: Populate
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/openf1/populate
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
